ssh root@45.93.139.61
enter password
then go to root of the project:
cd /root/eumaps


USEFUL COMMANDS:
pm2 status
pm2 logs eumaps
pm2 restart eumaps
cd /root/eumaps
git pull



CASE 1: normal code changes
On your computer:
	git add .
	git commit -m "update"
	git push
On VPS:	ssh root@45.93.139.61
	cd /root/eumaps
	git pull
	pm2 restart eumaps
	pm2 status



CASE 2:  if package.json or package-lock.json changed.
On your computer:
	git add .
	git commit -m "update packages"
	git push
On VPS:	ssh root@45.93.139.61
	cd /root/eumaps
	git pull
	npm install
	pm2 restart eumaps
	pm2 status



CASE 3: .env changes
On VPS:	ssh root@45.93.139.61
	cd /root/eumaps
	nano .env
Edit values, then save:
	Ctrl + O
	Enter
	Ctrl + X
Restart:pm2 restart eumaps
	pm2 status

Hello, while testing your service as a guest user, I found two issues worth reviewing.

Issue 1 – A guest can repeatedly upload/convert files, consuming heavy backend resources. I converted 30MB PDF to 175MB JPG archive (205MB bandwidth), repeatable over and over.

Issue 2 – After conversion, download can be clicked unlimited times. DevTools confirms each click re-fetches from server instead of a cached copy.

Fixes: limit guest conversions, remove guest usage or serve repeats from a local blob not from server. I'd appreciate consideration for a bug reward or goodwill bounty if this is useful to your team. Best regards